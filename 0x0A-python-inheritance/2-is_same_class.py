#!/usr/bin/python3
"""a function that check if an
object is an instance
"""


def is_same_class(obj, a_class):
    """a function that check if a
    object is a is instance of a class"""
    if type(obj) is a_class:
        return True
    else:
        return False
